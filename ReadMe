Я реализовал копирование объектов через методы, так как в реальной жизни мы не всегда копируем объект целиком и если есть методы геттеры и сетторы, 
то значит мы собираемся предоставить доступ к полям объекта через эти методы. Также я не реализовал копирование дочерних объектов, теоретически такое необходимо,
для копирования всего дерева объектов, но тогда необходимо задавать аннтоацию на поля, типа @children и для таких полей создавать новый объект копированием через рекурсию,
а для обычных полей ссылок копировать только ссылку. Этого не было в описании к задаче, поэтому я поленился писать дополнительный код.
